# 单向导出 Markdown，sqlite 为唯一事实源

里程碑一的 markdown 导出采用**单向导出**：sqlite 是唯一事实源，markdown 只是导出产物，手动触发或应用启动时自动导出到用户可配置的 git 目录。外部修改 markdown 不会回流到应用。

选择单向而非双向同步，是因为里程碑一的目标是快速跑通框架，双向同步的冲突处理、文件监听、删除同步会显著拖慢进度；单向导出已满足"AI 可读、git 可追踪历史"的核心诉求。导出格式会为每条 Todo 带稳定 id，为未来升级为双向同步留好升级路径。
