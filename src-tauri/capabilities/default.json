{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：core 默认权限 + 外壳插件最小面。opener/dialog 仅由 Rust 侧自定义命令调用（不经 JS IPC），故不授予插件默认集（最小权限，见评审）",
  "windows": ["main", "quickadd"],
  "permissions": ["core:default"]
}
